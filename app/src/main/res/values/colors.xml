<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Mirrors the Ink token in Color.kt. The starting window is drawn by the
         platform before any Compose content exists, so this value has to be
         repeated here or the app opens on a colour it never uses again. -->
    <color name="ink">#FF14120F</color>
</resources>
